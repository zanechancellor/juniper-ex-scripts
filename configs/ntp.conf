system {
    replace: ntp {
        {% for key in ntpkey %}
        authentication-key {{ key.id }} type {{ key.type }} value "{{ key.value }}";
        {% endfor %}
        {% for server in ntpserver %}
        server {{ server.address }}{% if server.key %} key {{server.key}}{% endif %}{% if server.primary %} prefer{% endif %};
        {% endfor %}
        {% if trustedkeys|length > 0 %}
        trusted-key [{% for key in trustedkeys %} {{ key }} {% endfor %}];
        {% endif %}
    }
}